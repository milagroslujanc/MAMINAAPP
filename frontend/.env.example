# Vacío = mismas rutas /api (nginx hace proxy en Docker)
# Local con Vite: también vacío si usas el proxy de vite.config.js
# Railway (obligatorio en build): VITE_API_URL=https://tu-backend.up.railway.app
VITE_API_URL=
